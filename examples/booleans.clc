-- Chruch booleans

let true   = \x.\y.x
let false  = \x.\y.y

let not = \x.x false true

let and = \a.\b.a b false
let or  = \a.\b.a true b
let xor = \a.\b.a (not b) b
let if  = \p.\then.\else.p then else